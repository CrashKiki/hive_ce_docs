# Logging

Hive CE has a configurable logging system

```dart
// Configure the overall logging level
HiveLogger.level = HiveLoggerLevel.error;

// Disable the unsafe isolate warning
HiveLogger.unsafeIsolateWarning = false;
```
